# 前端八股文面经大全：腾讯CSIG实习面（2026-04-10）·面经深度解析

- **来源**: CSDN·木斯佳
- **原始链接**: https://blog.csdn.net/weixin_50077637/article/details/160116965
- **抓取时间**: 2026-04-14
- **面试公司**: 腾讯CSIG
- **面试岗位**: 前端一面（实习）

## 面试问题清单

### 八股拷打
1. 流式输出的原理
2. 为什么用SSE不用axios，他们在http上有什么区别
3. 讲讲http1,1.1,2,3，他们是为了做什么优化才更新的
4. 埋点的意义
5. vue的计算属性的原理
6. 有了解过浏览器安全的知识吗
7. XSS是什么知道吗
8. UDP是怎么保证通讯可靠
9. http请求的流程知道吗
10. 有用框架写过东西吗
11. 现在工作中你写逻辑还是样式
12. 流式输出前端怎么样从后台拿到数据
13. 有没有什么做的不好的地方
14. JS事件循环了解过吗？优点缺点？
15. 为什么你说AI对你埋点的帮助不大
16. 有用过Vux这种的，什么场景下需要他
17. 你说到面包屑，面包屑只是弄个数组存起来然后拿出来用吗？他的难点在哪
18. 虚拟列表的原理，有没有别的方案
19. 你说的insectionObserver是什么，他怎么实现虚拟列表
20. 你觉得最考验你的部分是什么
21. JS闭包了解过吗？他是为了解决什么问题存在的？

### 手撕
22. 高性能Top K频繁子串查询系统

---

## 逐题深度解析

### 一、流式输出的原理

流式输出是指服务端分块发送数据，客户端逐步接收和渲染。

**原理**：
1. 服务端设置响应头 `Content-Type: text/event-stream` 或 `Transfer-Encoding: chunked`
2. 客户端使用 `EventSource` 或 `fetch` + `ReadableStream` 接收
3. 数据以块（chunk）形式传输，每收到一块就立即处理，不等全部数据
4. 适用于大文件下载、AI对话、实时日志

```javascript
// 客户端流式接收
const response = await fetch('/api/stream')
const reader = response.body.getReader()
const decoder = new TextDecoder()

while (true) {
  const { done, value } = await reader.read()
  if (done) break
  const chunk = decoder.decode(value)
  console.log('收到数据块:', chunk)
}
```

---

### 二、为什么用SSE不用axios？他们在HTTP上有什么区别？

**核心区别**：
- **axios**：请求-响应模式，一次请求返回完整数据，不支持流式
- **SSE**：建立持久连接，服务端可多次推送数据，客户端逐块接收

**HTTP层面区别**：
- axios使用标准HTTP请求，响应完成后连接关闭（或Keep-Alive复用）
- SSE使用 `text/event-stream` 类型，连接保持打开，服务端持续写入数据

**为什么AI场景用SSE**：AI生成需要逐字输出，SSE支持流式，axios只能等全部生成完再返回。

---

### 三、HTTP版本演进

| 版本 | 核心特性 | 解决的问题 | 遗留问题 |
|------|----------|-----------|----------|
| **HTTP/1.0** | 短连接，每次请求建立TCP | 基础通信 | 连接开销大 |
| **HTTP/1.1** | 长连接（Keep-Alive）、管道化、host头 | 减少连接开销 | 队头阻塞 |
| **HTTP/2** | 多路复用、头部压缩、服务器推送、二进制分帧 | 解决队头阻塞、减少头部冗余 | TCP队头阻塞 |
| **HTTP/3** | 基于UDP（QUIC）、0-RTT连接、独立流 | 解决TCP队头阻塞、减少握手延迟 | 部署较新 |

**关键演进**：1.0→1.1解决连接复用；1.1→2解决并发请求；2→3解决TCP队头阻塞。

---

### 四、埋点的意义

1. **产品决策**：分析用户行为（点击、停留、转化），指导产品迭代
2. **性能监控**：采集页面加载时间、接口响应时间、错误率
3. **用户画像**：收集用户偏好、使用习惯
4. **A/B测试**：对比不同方案的效果
5. **异常告警**：发现JS错误、接口异常

---

### 五-二十一（省略，详见原文）

包括：Vue计算属性原理、浏览器安全、XSS、UDP可靠性、HTTP请求流程、流式前端获取、事件循环、Vuex使用场景、面包屑难点、虚拟列表、IntersectionObserver、闭包等。

---

### 手撕：高性能Top K频繁子串查询系统

**解题思路**：
1. 滑动窗口枚举所有子串
2. 使用哈希表统计频率
3. 使用最小堆维护Top K

```javascript
function topKFrequentSubstrings(s, k, minLen = 1, maxLen = 10) {
  const freq = new Map()
  for (let len = minLen; len <= maxLen; len++) {
    for (let i = 0; i <= s.length - len; i++) {
      const sub = s.substring(i, i + len)
      freq.set(sub, (freq.get(sub) || 0) + 1)
    }
  }
  const heap = []
  for (const [sub, count] of freq) {
    if (heap.length < k) {
      heap.push({ sub, count })
      heap.sort((a, b) => a.count - b.count)
    } else if (count > heap[0].count) {
      heap[0] = { sub, count }
      heap.sort((a, b) => a.count - b.count)
    }
  }
  return heap.sort((a, b) => b.count - a.count)
}
```

**优化方向**：后缀数组+LCP、字典树、滚动哈希（Rabin-Karp）。复杂度：暴力O(n²)，优化后O(n log n)。
